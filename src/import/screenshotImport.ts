import type { BannerEmblems, BoardLayoutId, BoardState, DataBundle, EmblemState, MenuState, QualityTier, Role, SlotColor, StatName, TraitName } from '../domain/types.js';
import { BOARD_LAYOUTS, isLegalStat } from '../domain/rules.js';
import { ACTION_BY_ID, ACTION_CATALOG, cloneAction } from '../data/actionCatalog.js';
import { parseScreenshotLocally, type LocalScreenshotOcrMetrics } from './localScreenshotOcr.js';
import { refineUncertainEmblemStats } from './emblemOcrRefinement.js';

const ROLES: readonly Role[]=['core','mid','support'];
const TRAITS: readonly TraitName[]=['Fractal','Friendly','Vampiric','Unique','Benevolent'];
const REVIEW_THRESHOLD=.9;
export interface ScreenshotFieldConfidence { path:string;confidence:number; }
export interface RawScreenshotEmblem { position:number;color:SlotColor;stat:StatName;qualityTier:QualityTier;trait:TraitName; }
export interface RawScreenshotBanner { selectedTeam:string;emblems:RawScreenshotEmblem[]; }
export interface RawScreenshotImport { layoutId:BoardLayoutId;banners:Record<Role,RawScreenshotBanner>;operationIds:[string|null,string|null,string|null];tokensRemaining?:number;fieldConfidence?:ScreenshotFieldConfidence[];warnings?:string[]; }
export interface ScreenshotImportRequest { imageDataUrl:string;teamsByRole:Record<Role,string[]>;actions:Array<{id:string;label:string}>; }
export interface ValidatedScreenshotImport { board:BoardState;menu:MenuState;tokensRemaining?:number;warnings:string[];lowConfidenceFields:ScreenshotFieldConfidence[];requiresReview:boolean; }
let lastLocalOcrMetrics:LocalScreenshotOcrMetrics|undefined;
export function getLastLocalOcrMetrics():LocalScreenshotOcrMetrics|undefined{return lastLocalOcrMetrics;}
function assertRecord(v:unknown,label:string):asserts v is Record<string,unknown>{if(!v||typeof v!=='object'||Array.isArray(v))throw new Error(`${label} is missing or invalid.`);}
function asLayoutId(v:unknown):BoardLayoutId{if(v!=='legacy_3'&&v!=='expanded_5')throw new Error('Screenshot parser returned an unsupported board layout.');return v;}
function asTeam(v:unknown,role:Role,data:DataBundle):string{if(typeof v!=='string')throw new Error(`Screenshot parser did not return a ${role} team.`);const legal=new Set(data.players.filter(p=>p.role===role).map(p=>p.team));if(!legal.has(v))throw new Error(`Screenshot parser returned unknown ${role} team: ${v}.`);return v;}
function asTier(v:unknown,path:string):QualityTier{if(!Number.isInteger(v)||Number(v)<1||Number(v)>5)throw new Error(`${path} has an invalid quality tier.`);return Number(v) as QualityTier;}
function asTrait(v:unknown,path:string):TraitName{if(typeof v!=='string'||!TRAITS.includes(v as TraitName))throw new Error(`${path} has an invalid trait.`);return v as TraitName;}
function confidences(v:unknown):ScreenshotFieldConfidence[]{if(!Array.isArray(v))return[];const out:ScreenshotFieldConfidence[]=[];for(const row of v){if(!row||typeof row!=='object')continue;const c=row as {path?:unknown;confidence?:unknown};if(typeof c.path==='string'&&typeof c.confidence==='number'&&Number.isFinite(c.confidence))out.push({path:c.path,confidence:Math.max(0,Math.min(1,c.confidence))});}return out;}
export function screenshotImportRequest(imageDataUrl:string,data:DataBundle):ScreenshotImportRequest{const teamsByRole=Object.fromEntries(ROLES.map(role=>[role,[...new Set(data.players.filter(p=>p.role===role).map(p=>p.team))].sort()])) as Record<Role,string[]>;return{imageDataUrl,teamsByRole,actions:ACTION_CATALOG.map(a=>({id:a.id,label:a.label}))};}
export function validateScreenshotImport(raw:unknown,data:DataBundle,currentBoard:BoardState,currentMenu:MenuState):ValidatedScreenshotImport{assertRecord(raw,'Screenshot import');const layoutId=asLayoutId(raw.layoutId);assertRecord(raw.banners,'Screenshot banners');const layout=BOARD_LAYOUTS[layoutId],board={} as BoardState;for(const role of ROLES){const rb=raw.banners[role];assertRecord(rb,`${role} banner`);const selectedTeam=asTeam(rb.selectedTeam,role,data),re=rb.emblems;if(!Array.isArray(re))throw new Error(`${role} emblems are missing.`);const slots=layout.roles[role];if(re.length!==slots.length)throw new Error(`${role} has ${re.length} parsed emblems but ${layoutId} requires ${slots.length}.`);const emblems=slots.map((slot,index)=>{const c=re[index];assertRecord(c,`${role} emblem ${index+1}`);if(c.position!==slot.index)throw new Error(`${role} emblem ${index+1} has the wrong position.`);if(c.color!==slot.color)throw new Error(`${role} emblem ${index+1} color conflicts with the ${layoutId} layout.`);if(typeof c.stat!=='string'||!isLegalStat(slot.color,c.stat as StatName))throw new Error(`${role} emblem ${index+1} returned an illegal ${slot.color} stat.`);return{id:`${role}-${slot.index}`,position:slot.index,color:slot.color,stat:c.stat as StatName,qualityTier:asTier(c.qualityTier,`${role} emblem ${index+1}`),trait:asTrait(c.trait,`${role} emblem ${index+1}`)} satisfies EmblemState;}) as BannerEmblems;board[role]={role,selectedTeam,expectedSeries:currentBoard[role].expectedSeries,emblems};}if(layoutId!=='legacy_3')board.layoutId=layoutId;if(!Array.isArray(raw.operationIds)||raw.operationIds.length!==3)throw new Error('Screenshot parser must return exactly three offered-action slots.');const fc=confidences(raw.fieldConfidence),warnings=Array.isArray(raw.warnings)?raw.warnings.filter((x):x is string=>typeof x==='string'&&x.trim().length>0):[],resolved:string[]=[];raw.operationIds.forEach((v,index)=>{if(v===null){resolved.push(currentMenu[index]!.id);if(!fc.some(x=>x.path===`operationIds.${index}`))fc.push({path:`operationIds.${index}`,confidence:0});if(!warnings.some(x=>x.includes(`Action ${index+1}`)))warnings.push(`Action ${index+1} was not visible; existing action preserved until reviewed.`);return;}if(typeof v!=='string'||!ACTION_BY_ID.has(v))throw new Error(`Screenshot parser returned unknown action: ${String(v)}.`);resolved.push(v);});const visible=raw.operationIds.filter((v):v is string=>typeof v==='string');if(new Set(visible).size!==visible.length)throw new Error('Screenshot parser returned duplicate offered actions.');const menu=resolved.map(id=>cloneAction(ACTION_BY_ID.get(id)!)) as MenuState;let tokensRemaining:number|undefined;if(raw.tokensRemaining!==undefined){if(!Number.isInteger(raw.tokensRemaining)||Number(raw.tokensRemaining)<0)throw new Error('Screenshot parser returned an invalid token count.');tokensRemaining=Number(raw.tokensRemaining);}const lowConfidenceFields=fc.filter(x=>x.confidence<REVIEW_THRESHOLD),result:ValidatedScreenshotImport={board,menu,warnings,lowConfidenceFields,requiresReview:warnings.length>0||lowConfidenceFields.length>0};if(tokensRemaining!==undefined)result.tokensRemaining=tokensRemaining;return result;}
export async function fileToScreenshotDataUrl(file:File,maxDimension=1800):Promise<string>{if(!file.type.startsWith('image/'))throw new Error('Choose an image screenshot (PNG, JPEG, or WebP).');const source=await new Promise<HTMLImageElement>((ok,no)=>{const image=new Image(),url=URL.createObjectURL(file);image.onload=()=>{URL.revokeObjectURL(url);ok(image);};image.onerror=()=>{URL.revokeObjectURL(url);no(new Error('The selected screenshot could not be decoded.'));};image.src=url;});const scale=Math.min(1,maxDimension/Math.max(source.naturalWidth,source.naturalHeight)),canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round(source.naturalWidth*scale));canvas.height=Math.max(1,Math.round(source.naturalHeight*scale));const ctx=canvas.getContext('2d');if(!ctx)throw new Error('Canvas image processing is unavailable in this browser.');ctx.drawImage(source,0,0,canvas.width,canvas.height);return canvas.toDataURL('image/jpeg',.9);}
async function visionFallback(file:File,data:DataBundle):Promise<RawScreenshotImport>{const endpoint=document.querySelector<HTMLMetaElement>('meta[name="screenshot-import-endpoint"]')?.content;if(!endpoint)throw new Error('Local OCR could not confidently reconstruct this screenshot.');const imageDataUrl=await fileToScreenshotDataUrl(file),response=await fetch(endpoint,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(screenshotImportRequest(imageDataUrl,data))});if(!response.ok){const detail=await response.text().catch(()=> '');throw new Error(`Screenshot recognition failed (${response.status})${detail?`: ${detail.slice(0,240)}`:'.'}`);}return await response.json() as RawScreenshotImport;}
export async function requestScreenshotImport(file:File,data:DataBundle):Promise<RawScreenshotImport>{lastLocalOcrMetrics=undefined;try{const local=await parseScreenshotLocally(file,data);lastLocalOcrMetrics=local.metrics;return await refineUncertainEmblemStats(file,data,local.result);}catch(localError){const endpoint=document.querySelector<HTMLMetaElement>('meta[name="screenshot-import-endpoint"]')?.content;if(!endpoint)throw localError;return await visionFallback(file,data);}}
