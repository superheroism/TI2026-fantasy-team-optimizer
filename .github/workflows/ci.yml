name: Regenerate stat-row retry

on:
  push:
    branches:
      - agent/screenshot-stat-row-retry

permissions:
  contents: write

jobs:
  regenerate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out follow-up branch
        uses: actions/checkout@v4
        with:
          ref: agent/screenshot-stat-row-retry
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Apply stat-row retry fix
        run: node scripts/apply-stat-row-retry.mjs

      - name: Build and validate
        run: |
          npm run build
          npm run test:ci

      - name: Commit source, tests, and generated artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/import/emblemOcrRefinement.ts src/ui/boardView.ts tests/stat-row-retry-ui.test.mjs build docs
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Retry low-confidence stats from a tight native crop"
          git push origin HEAD:agent/screenshot-stat-row-retry
