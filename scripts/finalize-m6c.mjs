import fs from 'node:fs';
const base='42a1a7ec7553e7d6df5f4d3c5576417fcdbbb35a',cal=JSON.parse(fs.readFileSync('benchmarks/m6c-expanded-calibration-results.json','utf8')),sel=JSON.parse(fs.readFileSync('benchmarks/m6c-selection.json','utf8')),hold=fs.existsSync('benchmarks/m6c-expanded-holdout-results.json')?JSON.parse(fs.readFileSync('benchmarks/m6c-expanded-holdout-results.json','utf8')):null;
const outcome=hold?.outcome??'B',selected=sel.selectedCandidate;const chosen=cal.candidates.find(x=>x.policy.id===selected);const fmt=x=>x===null||x===undefined?'n/a':Number(x).toFixed(4);const pct=x=>x===null||x===undefined?'n/a':`${(100*x).toFixed(1)}%`;
let md=`# M6C — Expanded-Board t=2 Approximation Calibration\n\n**Status:** Outcome ${outcome}${outcome==='A'?' — bounded expanded t=2 approximation validated':' — approximation family failed fidelity/performance gate'}  \n**Frozen base:** \`${base}\`  \n**Authoritative runtime:** GitHub Actions, Node 22/Linux, isolated child processes with \`--expose-gc\`\n\n## Frozen design\n\nCalibration and holdout corpora plus the compact candidate grid were committed before candidate results. M6C evaluated one preregistered family: adaptive root refinement with K = 2, 4, or 6 exact second-token board-action refinements. Every legal root board action is still screened with exact one-step mechanics/scoring, while stop and menu reroll remain represented. Unrefined board roots stop after the first action. No stochastic scenario approximation is used inside states that are evaluated.\n\nThe old M5D progressive widening mechanism was not reused mechanically at t=2 because at one token remaining its shallow and deep action values both terminate after the action; it therefore does not avoid the expanded terminal frontier that M6B identified.\n\nAcceptance thresholds were frozen in \`benchmarks/m6c-expanded-candidates.json\` before calibration: 100% completion, >=94% root-action agreement, max expected-score oracle regret <=100, max target-probability regret <=0.005, median speedup >=1.30x, P90 speedup >=1.50x, and median structural work avoided >=20%.\n\n## Calibration\n\n| Candidate | Qualifies | Root agreement | Max expected regret | Max target regret | Median speedup | P90 speedup | Median work avoided |\n|---|---:|---:|---:|---:|---:|---:|---:|\n`;
for(const c of cal.candidates)md+=`| ${c.policy.id} | ${c.qualifies?'yes':'no'} | ${pct(c.summary.rootActionAgreement)} | ${fmt(c.summary.maximumExpectedScoreOracleRegret)} | ${fmt(c.summary.maximumTargetProbabilityOracleRegret)} | ${fmt(c.summary.medianSpeedup)}x | ${fmt(c.summary.p90Speedup)}x | ${pct(c.summary.medianStructuralWorkAvoided)} |\n`;
md+=`\nFrozen calibration selection: **${selected??'none'}**.\n`;
if(hold)md+=`\n## One-shot holdout\n\nThe holdout was consumed only after \`${selected}\` was frozen from calibration. It achieved ${pct(hold.summary.rootActionAgreement)} root-action agreement, max expected-score regret ${fmt(hold.summary.maximumExpectedScoreOracleRegret)}, max target-probability regret ${fmt(hold.summary.maximumTargetProbabilityOracleRegret)}, median speedup ${fmt(hold.summary.medianSpeedup)}x, P90 speedup ${fmt(hold.summary.p90Speedup)}x, and median structural work avoided ${pct(hold.summary.medianStructuralWorkAvoided)}. Holdout gate: **${hold.passed?'PASS':'FAIL'}**.\n`;
else md+=`\n## Holdout\n\nNot consumed because no calibration candidate qualified.\n`;
md+=`\n## Decision\n\n`+(outcome==='A'?`**Outcome A.** \`${selected}\` passed calibration and the one-shot M6C holdout. This validates only the measured \`expanded_5\`, t=2 operating envelope. M6C does **not** enable the approximation in production; any integration requires a separate package.\n`:`**Outcome B.** No candidate established the required fidelity/performance envelope. Exact expanded_5 t=2 remains the reference implementation.\n`)+`\n## Production isolation\n\nNo production engine source was changed to enable this approximation. Default layout remains \`legacy_3\`; production lookahead remains capped at two; legacy expected-score and target-probability behavior remain exact; the M5H holdout is untouched; target t=3/t=4 were not run.\n\n## Evidence\n\n- \`benchmarks/m6c-expanded-calibration-fixtures.json\` — frozen calibration corpus\n- \`benchmarks/m6c-expanded-holdout-fixtures.json\` — frozen holdout corpus\n- \`benchmarks/m6c-expanded-candidates.json\` — preregistered candidates and gates\n- \`benchmarks/m6c-expanded-calibration-results.json\` — exact oracle + candidate calibration evidence\n- \`benchmarks/m6c-selection.json\` — calibration-only frozen selection\n${hold?'- `benchmarks/m6c-expanded-holdout-results.json` — one-shot holdout evidence\n':''}`;
fs.writeFileSync('M6C_EXPANDED_T2_APPROXIMATION.md',md);
const perf=`\n\n## M6C expanded t=2 approximation calibration\n\nM6C calibrated adaptive root refinement against exact expanded_5 t=2 oracles. Frozen selection: ${selected??'none'}. Final outcome: **${outcome}**.${hold?` One-shot holdout root agreement ${pct(hold.summary.rootActionAgreement)}, median speedup ${fmt(hold.summary.medianSpeedup)}x, P90 speedup ${fmt(hold.summary.p90Speedup)}x, median structural work avoided ${pct(hold.summary.medianStructuralWorkAvoided)}.`:''} Production remains legacy_3 / t<=2 and the approximation is disabled by default. See \`M6C_EXPANDED_T2_APPROXIMATION.md\`.\n`;
let p=fs.readFileSync('PERFORMANCE.md','utf8');if(!p.includes('## M6C expanded t=2 approximation calibration'))fs.writeFileSync('PERFORMANCE.md',p.replace(/\s*$/,'')+perf);
const road=`\n\n---\n\n## M6C — Expanded t=2 approximation calibration\n\nM6C closes as **Outcome ${outcome}**.${outcome==='A'?` Adaptive root refinement \`${selected}\` passed the frozen calibration and one-shot expanded-board holdout gates. The validated envelope is experimental only; schedule a separate integration package before any production use.`:' The preregistered bounded approximation family did not pass the frozen fidelity/performance gates; retain exact expanded_5 t=2 as reference.'} Production remains \`legacy_3\` with horizon <=2. M5H holdout remains untouched and target t=3/t=4 remain frozen.\n`;
let r=fs.readFileSync('ENGINEERING_ROADMAP.md','utf8');if(!r.includes('## M6C — Expanded t=2 approximation calibration'))fs.writeFileSync('ENGINEERING_ROADMAP.md',r.replace(/\s*$/,'')+road);
